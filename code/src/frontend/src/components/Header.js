import React from 'react';

const Header = () => (
  <header className="app-header">
    <h1>Smarter Reconciliation and Anomaly Detection using Gen AI</h1>
  </header>
);

export default Header;